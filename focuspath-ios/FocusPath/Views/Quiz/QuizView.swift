import SwiftUI
struct QuizView: View {
    let passage: Passage
    let focusSeconds: Int
    var body: some View {
        Text("Quiz — coming in Task 8")
            .foregroundStyle(Theme.brown)
    }
}
